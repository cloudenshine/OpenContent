# OpenContent 0.6 数据与隐私说明

OpenContent 是桌面 Obsidian 插件加本地 Python Kernel。插件没有遥测、广告、远程统计或 OpenContent 云服务；市场试点记录表由使用者自行填写和决定是否分享。

## 本地保存

- 项目、捕获材料、知识、主张、证据、草稿、审查与批准保存为 Vault 内 Markdown/YAML。
- `.opencontent/` 保存任务状态、请求、响应和诊断日志，**其中可能包含全文材料和生成内容**；不要直接公开整个目录。
- 插件 `data.json` 保存运行路径、主动配置的 Skill 路径、默认署名和上次项目/稿件 ID。它不是加密凭据仓库。Agent 登录由其自身客户端管理。
- 捕获只复制用户选定笔记或选区；全文模式省略开头的 YAML 属性，并读取 `source` URL。相同材料去重，来源笔记不被改写。

## 外部处理

无 AI 捕获和人工维护不需要调用模型。用户启动创作、重审或重试后，Kernel 把当前项目目标、关联材料/知识/主张/证据/草稿、CONTENT.md 和主动配置的 Skills 交给 Agent。运行结果、成本与数据处理方式取决于用户配置的 Agent 及其服务商；“本地启动 CLI”不等于“模型在本地离线推理”。启动前可查看材料范围。

插件请求只指向其启动的 loopback Kernel。Kernel 的请求范围控制和 read-only Agent 执行模式不应理解为完全隔离机器其他可读文件的安全保证；请根据 Agent 自身配置处理敏感资料。

## 交接与退出

交接仅创建本地 Markdown 和校验收据，不自动向 Ailu 或发布平台传输。输出不附加审查署名和材料本地路径；来源 URL 的查询串/片段会移除。无法自动判断正文或 URL 路径是否包含私密内容，作者需在实际交付前检查；部分来源链接也可能需要补正。

停用或卸载插件不会清除 Markdown 内容。要保留完整运行诊断，请一起备份 `.opencontent/`；仅删除运行数据库不会删除权威内容。排错时优先分享脱敏错误和版本，不分享原始材料与登录信息。

## 微信公众号外部处理（0.4）

只有用户主动检查账号、上传封面、确认送草稿/发表或查询回执时，Kernel 才请求固定的 `https://api.weixin.qq.com`。凭据不会交给 Agent、写入领域 Markdown、返回界面或放入安装包。AppSecret 在 Windows 使用当前登录用户的 DPAPI 加密保存于 `.opencontent/credentials/`；其他平台可使用明确配置的环境变量。访问令牌只在内存缓存；不跟随重定向，不使用环境代理。备份包含加密凭据，但换用户/系统后通常需要重新配置。

确认投递会发送预览中显示的标题、作者、摘要、正文及封面素材 ID；确认上传封面会发送所选 Vault 图像。投递回执、内容快照、反馈及恢复文件可能包含全文，不应直接公开。反馈数据目前由人输入，不自动读取留言或微信统计。

删除插件不会删除已送入微信的草稿或已发表文章。撤回、删除、后台改文与群发需在微信后台按账号能力处理；本插件不会静默替用户执行。
# v0.5 项目指令台与发现

目标推荐只在本地扫描允许范围内的 Markdown。用户选择材料后，项目指令会把该项目内容、最近 12 轮对话及明确配置的 Skills 交给所选本地 CLI；该 CLI 可能调用其模型服务。完整对话保存在 Vault 的 OpenContent-Workspace 目录，CLI 日志保存在 .opencontent/runs。

配图模式允许 Codex 在独立运行目录使用其现有图像能力，成功文件导入 Vault 附件目录。Claude 当前仅生成文字方案。发现的 CLI 路径不等于已登录或具备图像能力。插件不会自动公开发表对话、提案或图片。

## 跨资料选题（0.6）

打开选题窗口只准备本地范围。点击“综合这批资料，生成选题灵感”后，最多 60 篇去重笔记的标题和摘录（每篇正文约 2400 字符以内）、选题方向进入所选 CLI 的分类请求；提炼出的资料卡、已有项目的标题/目标/论点及最多三篇稿件的短摘录进入第二阶段比对请求。界面先显示数量、目录和截取范围，操作按钮旁说明发送行为。这里不要求先把笔记捕获进项目。

摘要并不保证去除隐私。分类和综合可能由 CLI 的远端模型处理，按其登录与服务条款执行。范围快照、源笔记路径、哈希、摘录和模型结果保存于 `.opencontent/ideation/`；可读灵感报告保存于 `OpenContent-Workspace/Ideation/`。它们是本地工作资料，不自动附到发表正文。


## 资料范围与恢复（0.7）

目录清单和范围预览只在本地读取。用户可选择参与的目录（包含其子目录）及数量上限，默认为 24 份、最多 60 份；根目录选项仅包含根层笔记。界面先显示范围内篇数和实际取样数量，再由用户开始模型任务。前后两个阶段仍按上文的范围发送摘录、分类结果和既有项目摘要；方向及目录选择保存在插件设置中。

重试先核查来源和项目版本；分类检查点同时校验请求哈希和原文编号，仍有效时只重新发送综合请求。缓存损坏或格式不兼容时重新分类。旧失败记录保留，重试建立新任务。安装包的 kernel 只携带程序、模板和依赖清单，不包含用户运行日志、Vault 内容或凭据。
